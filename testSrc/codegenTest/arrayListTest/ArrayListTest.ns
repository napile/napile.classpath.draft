package codegenTest.arrayListTest

import napile.collection.ArrayList
import napile.reflect.Class
import napile.logging.Logger

/**
 * @author VISTALL
 */
class ArrayListTest
{
	static meth main(val arg : Array<String>)
	{
		var l = ArrayList<Int>()
		l.add(1)
		l.add(2)
		l.add(3)
		l.add(4)
		l.add(5)
		l.add(6)

		Logger.System.info('size is ' + l.size())
		for(c in 0..7)
			Logger.System.info(l[c].toString())
	}
}