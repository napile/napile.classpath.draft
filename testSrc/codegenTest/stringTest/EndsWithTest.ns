package codegenTest.stringTest

import napile.logging.Logger

/**
 * @author VISTALL
 */
class EndsWithTest
{
	static meth main(val arg : Array<String>)
	{
		var str = 'endsWith'

		Logger.System.info('str: ' + str + ' : result(With): ' + str.endsWith('With'))

		Logger.System.info('str: ' + str + ' : result(ends): ' + str.endsWith('ends'))
	}
}