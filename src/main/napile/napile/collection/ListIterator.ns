package napile.collection

/**
 * @author VISTALL
 */
class ListIterator<E> : Iterator<E>
{
	local val list : List<E>
	local var index : Int = 0

	this(list) : Iterator<E>()

	override meth hasNext() : Bool = list.size() != index

	override meth next() : E = list[index ++]
}