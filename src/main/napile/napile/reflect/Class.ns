package napile.reflect

import napile.reflect.Class
import napile.io.Console
import napile.annotation.Immutable

/**
 * @author VISTALL
 */
class Class<E> : Member
{
	final var variables : Array<Variable>

	final var methods : Array<Method>

	final var macros : Array<Macro>

	local this(owner : Class<Any>?, name : String) : Member(owner, name)

	native meth variables.lazy() : Array<Variable>

	native meth methods.lazy() : Array<Method>

	native meth macros.lazy() : Array<Macro>
}