package napile.reflect

/**
 * @author VISTALL
 */
class TypeInfo : AnnotationOwner
{
	val typeConstructor : TypeConstructor

	val nullable : Bool

	local this(typeConstructor, nullable, val annotations : Array<Any>) : AnnotationOwner(annotations)
}