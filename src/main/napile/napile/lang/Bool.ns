package napile.lang

import napile.annotation.Enumable

/**
 * @author VISTALL
 */
class Bool
{
	//@Enumable
	static final var TRUE : Bool = Bool()

	//@Enumable
	static final var FALSE : Bool = Bool()

	local this()

	meth not() : Bool = if(this) FALSE else TRUE

	override meth toString() = if(this == TRUE) "true" else "false"
}