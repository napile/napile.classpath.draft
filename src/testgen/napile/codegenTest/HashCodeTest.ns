package codegenTest

import napile.io.Console

/**
 * @author VISTALL
 */
class HashCodeTest
{
	this()

	static meth main(val arg : Array<String>)
	{
		Console.writeLine(HashCodeTest().toString())
		Console.writeLine(HashCodeTest().toString())
		Console.writeLine(HashCodeTest().toString())
		Console.writeLine(HashCodeTest().toString())
		Console.writeLine(HashCodeTest().toString())
	}
}