package codegenTest.whenTest

import napile.logging.Logger

/**
 * @author VISTALL
 */
class WhenIsTest
{
	static meth main(val arg : Array<String>)
	{
		val i : Any = 1

		when (i)
		{
			is Int -> Logger.System.info('i is Int')
			else -> Logger.System.info('error')
		}
	}
}