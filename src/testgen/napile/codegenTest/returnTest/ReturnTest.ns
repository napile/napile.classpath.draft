package codegenTest.returnTest

import napile.logging.Logger

/**
 * @author VISTALL
 */
class ReturnTest
{
	static meth main(val arg : Array<String>)
	{
		Logger.System.info("before return");

		if (true)
		{
			Logger.System.info("is returned")
			return
		}

		Logger.System.info("after return")
	}
}